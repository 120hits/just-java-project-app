<?xml version="1.0" encoding="utf-8"?>


<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"

    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    tools:context=".MainActivity">

    <TextView
        android:id="@+id/quantity"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:textSize="16sp"
        android:padding="16dp"
        android:textAllCaps="true"
        android:layout_marginBottom="16dp"
        android:text="@string/quantity"/>

    <TextView
        android:id="@+id/quantity_text_view"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:padding="16dp"
        android:textColor="@android:color/black"
        android:text="@string/zero"
        />

    <TextView
        android:id="@+id/price"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:textSize="16sp"
        android:padding="16dp"
        android:textAllCaps="true"
        android:layout_marginBottom="16dp"
        android:text="@string/price"/>

    <TextView
        android:id="@+id/price_text_view"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:padding="16dp"
        android:textColor="@android:color/black"
        android:text="@string/price_text_view"
        />



    <Button
        android:id="@+id/ORDER_button"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="16dp"
        android:text="@string/ORDER_button"
        android:onClick="submitOrder"/>

</LinearLayout>


<!--
 ok like before we have deleted the constraint layout for now and we have reverted
    to our older method of creating layouts
 For the just java app we are working with a linear layout
    thus the orientation for this is vertical
 Changed the default hello world to quantity
    as we hard code the desired text into the display
        we also have to create our string tag
        inside res/values/string conjunction with the text view
        so we have to first create a string tag
        name the string
        write the desired text
    Then we call the string name from our text view 1
Text view 1 we have added
    id name ,
    width & height = wrap content ,
    text size = 16sp, padding = 16dp ,
    textAllCaps = true
    layout margin bottom = 16 dp, and
    the name of the string @string/name
 Text view 2 we added
    id name ,
    width & height = wrap content ,
    padding = 16dp ,
    textColor = "@android:color/black"
    the name of the string @string/name
 Text view 3 we added
    same as text view 1
    but now we set the name tp price
    the auto caps is still there
 Text view 4 we added
    same as text view 2
    with now with $0
 Button tag we added
    id name
    width & height = wrap content
    layout margin top = 16dp
    onClick = "submitOrder"

 -->